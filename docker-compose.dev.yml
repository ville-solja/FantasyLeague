version: "3.8"

services:
  backend:
    build: .
    volumes:
      - ./backend:/app
      - ./frontend:/app/frontend
      - ./data:/app/data
    environment:
      - SCHEDULE_SHEET_URL=${SCHEDULE_SHEET_URL}
